<h1>u did it</h1>

<%=@item.name%>

<% @item.packagings.each do |packaging|%>
<p><%= packaging.category %></p>
<% packaging.rules.each do |rule| %>

  <% if rule.borough_id == @user.borough_id %>
  <% if rule.is_recycled == true%>
  <p><%= "#{@item.name} can be recycled in #{@user.borough.name} ! "%></p>
  <% end %>
  <% end %>

  <% end %>
<% end %>
